class TowersOfHanoi {
  run() {
    //  until all the discs are on either tower 2 or tower 3
    // prompt player for move
    // check if move is valid
      //  move is valid if its a disc going onto an empty space or
      //  its  a disc going onto a larger disk
    // make move requested if valid
    // else prompt user again with error messaage
  }
}
